# Notifications: emails and alerts. Declares Accounts and the root package "."
# (framework base classes). It deliberately does NOT declare Billing, yet its
# code references Billing::Invoice - an intentional static boundary violation
# for MonolithLens and Packwerk to catch.
enforce_dependencies: true
dependencies:
  - "."
  - packs/accounts
